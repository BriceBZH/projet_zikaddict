{% extends 'base.html.twig' %}

{% block title %}UserAlbumFormat index{% endblock %}

{% block body %}
    <h1>UserAlbumFormat index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Type</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for user_album_format in user_album_formats %}
            <tr>
                <td>{{ user_album_format.id }}</td>
                <td>{{ user_album_format.type }}</td>
                <td>
                    <a href="{{ path('app_user_album_format_show', {'id': user_album_format.id}) }}">show</a>
                    <a href="{{ path('app_user_album_format_edit', {'id': user_album_format.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_user_album_format_new') }}">Create new</a>
{% endblock %}

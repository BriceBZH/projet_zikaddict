{% extends 'base.html.twig' %}

{% block title %}UserAlbumFormat{% endblock %}

{% block body %}
    <h1>UserAlbumFormat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_album_format.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ user_album_format.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_user_album_format_index') }}">back to list</a>

    <a href="{{ path('app_user_album_format_edit', {'id': user_album_format.id}) }}">edit</a>

    {{ include('user_album_format/_delete_form.html.twig') }}
{% endblock %}

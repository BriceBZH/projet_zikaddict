<form method="post" action="{{ path('app_user_album_format_delete', {'id': user_album_format.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_album_format.id) }}">
    <button class="btn">Delete</button>
</form>

{% extends 'base.html.twig' %}

{% block title %}Nouvelle chanson{% endblock %}

{% block body %}
    <main class="forms_bis" id="container">
        <h2>Ajouter une chanson</h2>

        {{ include('songs/_form.html.twig') }}

        <a href="{{ path('profile', {'idUser': app.request.query.get('idUser')}) }}">Retour au profil</a>
    </main>
{% endblock %}
